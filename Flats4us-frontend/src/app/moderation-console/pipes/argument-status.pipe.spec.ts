import { ArgumentStatusPipe } from './argument-status.pipe';

describe('ArgumentStatusPipe', () => {
	it('create an instance', () => {
		const pipe = new ArgumentStatusPipe();
		expect(pipe).toBeTruthy();
	});
});
